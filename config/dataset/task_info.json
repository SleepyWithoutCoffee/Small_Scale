{
    "math": {
        "label_key": "answer",
        "prompt_key": "problem",
        "system": "",
        "template_args": {
            "prepend_prompt": "",
            "append_prompt": "Please reason step by step, and put your final answer within \\boxed{}."
        },
        "template": "{{prepend_prompt}}{{problem}}{{append_prompt}}"
    },
    "mcq": {
        "label_key": "answer",
        "prompt_key": "problem",
        "system": "",
        "template_args": {
            "prepend_prompt": "",
            "append_prompt": "\n\nPlease reason step by step and put your final answer (eg, A, B, C, D) within \\boxed{}, such as \"\\boxed{A}\"."
        },
        "template": "{{prepend_prompt}}{{problem}}{{append_prompt}}"
    },
    "code": {
        "label_key": "golden_solution",
        "prompt_key": "question_content",
        "system": "",
        "template_args": {
            "prepend_prompt": "You will be given a question (problem specification) and will generate a correct Python program that matches the specification and passes all tests.\n\n",
            "append_prompt": "",
            "instruction_with_starter_code": "You will use the following starter code to write the solution to the problem and enclose your code within delimiters.",
            "instruction_without_starter_code": "Read the inputs from stdin solve the problem and write the answer to stdout (do not directly test on the sample inputs). Enclose your code within delimiters as follows. Ensure that when the python program runs, it reads the inputs, runs the algorithm and writes output to STDOUT."
        },
        "template": "{{prepend_prompt}}Question: {{problem}}\n\n{% if starter_code %}{{instruction_with_starter_code}}\n```python\n{{starter_code}}\n```{% else %}{{instruction_without_starter_code}}\n```python\n# YOUR CODE HERE\n```{% endif %}\n\n{{append_prompt}}"
    }
}